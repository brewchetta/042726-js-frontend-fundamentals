        // element | duration | ease/animation curve | transformations
gsap.from("#logo", { duration: 1.5, ease: "bounce", scale: 0, rotation:45, y: -300 })

// tween ^^^
// a single small animation potentially part of a larger animation